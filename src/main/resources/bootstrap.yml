server:
  port: 8006
  
spring:
  application:
    name: UserMS
  cloud:
    consul:
      host: localhost
      config:
        fail-fast: false
        enabled: true
        prefixes: config
        defaultContext: application
        data-key: data
        profileSeparator: '::'
        format: YAML 
        
        